# Createlen Webhook Environment Configuration
# Copy this file to /etc/createlen/.env and fill in your values

# Port for the webhook listener to run on
PORT=3000

# GitHub App webhook secret (generate with: openssl rand -hex 32)
WEBHOOK_SECRET=your_webhook_secret_here

# GitHub App ID (found in your GitHub App settings)
GH_APP_ID=your_app_id_here

# Path to GitHub App private key (PEM format)
# Download from your GitHub App settings and place in /etc/createlen/
GH_PRIVATE_KEY_PATH=/etc/createlen/private-key.pem

# GitHub App installation ID (found in the installation URL)
# Format: https://github.com/settings/installations/{installation_id}
GH_INSTALLATION_ID=your_installation_id_here
