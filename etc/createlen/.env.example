# Createlen Webhook Listener Environment Variables
#
# Copy this file to /etc/createlen/.env and fill in the values
# DO NOT commit the actual .env file with real secrets!

# Port for the webhook listener (default: 3000)
PORT=3000

# Webhook secret for verifying GitHub webhook signatures
# Generate with: openssl rand -hex 32
# This MUST match the webhook secret configured in your GitHub App settings
WEBHOOK_SECRET=your_webhook_secret_here

# GitHub App credentials
# Get these from your GitHub App settings page
GH_APP_ID=your_github_app_id

# Path to your GitHub App private key file
# Download the private key from GitHub App settings and place it in /etc/createlen/
GH_PRIVATE_KEY_PATH=/etc/createlen/github-app-private-key.pem

# GitHub App Installation ID
# You can find this in the URL when you view your app installation
# Example: https://github.com/settings/installations/12345678
GH_INSTALLATION_ID=your_installation_id
