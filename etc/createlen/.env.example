# Createlen Environment Configuration
# Copy this file to /etc/createlen/.env and fill in the actual values

# Port for the webhook listener to bind to
PORT=3000

# GitHub webhook secret - generate with: openssl rand -hex 32
# This must match the secret configured in your GitHub App webhook settings
WEBHOOK_SECRET=your_webhook_secret_here

# GitHub App ID (found in your GitHub App settings)
GH_APP_ID=your_app_id_here

# Path to your GitHub App private key (PEM file)
# Download from your GitHub App settings and place in /etc/createlen/
GH_PRIVATE_KEY_PATH=/etc/createlen/private-key.pem

# GitHub App Installation ID
# Found in the URL when viewing your app installation: https://github.com/settings/installations/{ID}
GH_INSTALLATION_ID=your_installation_id_here
